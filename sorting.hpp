#ifndef SORTING_HPP
#define SORTING_HPP

#include <string>
#include <vector>

using namespace std;

void quickSort(vector<string>& arr, int low, int high);
void heapSort(vector<string>& arr);

#endif

